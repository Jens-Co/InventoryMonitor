pluginManagement {
    repositories {
        gradlePluginPortal()
    }
    plugins {
        id("io.github.goooler.shadow") version "8.1.7"// shadowing dependencies
    }
}

rootProject.name = 'InventoryMonitor'
